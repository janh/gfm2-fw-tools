# Glasfaser-Modem 2 Firmware Tools

For now, this only contains a Python script to decrypt a firmware image.

Theoretically, re-encrypting an image should also be possible, because only symmetric cryptography is used (unlike what the public key initially suggests).
