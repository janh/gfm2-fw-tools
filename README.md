# Glasfaser-Modem 2 Firmware Tools

For now, this contains Python scripts to decrypt and re-encrypt a firmware image.

Note that the actual partition data inside the image is signed using RSA, so this will probably not allow creating custom firmware.
